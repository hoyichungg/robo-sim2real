[package]
name = "drivers"
version = "0.1.0"
edition = "2024"

[dependencies]
r2_core = { path = "../r2_core", package = "robot-core" }

[features]
# 啟用 Raspberry Pi 實機驅動（需要在 RPi 上編譯執行）
rpi = ["dep:rppal"]

[dependencies.rppal]
version = "0.16"
optional = true
